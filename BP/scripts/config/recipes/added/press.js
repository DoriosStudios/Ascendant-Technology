/** @type {Readonly<Record<string, { output: string, required: number }>>} */
export const pressRecipeAdditions = Object.freeze({
    "utilitycraft:titanium": {
        output: "utilitycraft:titanium_plate",
        required: 2,
    },
});
